version: '3.8'

# Database and backend are now fully managed by Supabase.
# This file only runs the Angular frontend container.

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: budget_tracker_frontend
    ports:
      - '4200:80'
